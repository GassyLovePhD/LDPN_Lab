# Droplet Analysis Tool

This project analyzes CellProfiler output CSVs, counts droplets, normalizes data, and generates plots.

## Features

- Load multiple experiments from folder
- Calculate droplet counts and percentages
- Normalize to control
- Generate bar plots
- Streamlit interface for local folder or ZIP input (small datasets)

```bash
streamlit run app.py
